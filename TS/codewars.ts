// ---------Vowel Count-------------

// function getCount(str: string): number {
//   const vowelsList = ['a', 'e', 'i', 'o', 'u'];

//   return str.split('').reduce((acc: number, char: string): number => {
//     if (vowelsList.indexOf(char) >= 0) {
//       acc += 1;
//     }
//     return acc;
//   }, 0);
// }

// console.log(getCount('abracadabra'));
